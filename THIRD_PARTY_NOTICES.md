# Third-party notices

The macOS application bundles LiveKitWebRTC `144.7559.14`, distributed by
LiveKit from the `livekit/webrtc-xcframework` release repository. It contains
Google's WebRTC implementation. The pinned archive checksum and acquisition
URL are recorded in `tools/fetch-webrtc` and the dependency rationale is in
`docs/adr/0001-native-webrtc-stack.md`.

The complete WebRTC BSD license is copied from the verified XCFramework into
`Kernel Live View.app/Contents/Resources/LiveKitWebRTC-LICENSE.txt` during the
build.
